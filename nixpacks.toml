[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
    "npm ci",
    "cd server && npm ci"
]

[phases.build]
cmds = [
    "npm run build",
    "cd server && npx prisma generate"
]

[start]
cmd = "cd server && npx prisma migrate deploy && node src/index.js"
