# Virtual 1 · progreso

- Inicio: rama `curso-avanzado-v1` desde `8096b25`. Línea base: `vite build` pasa, `eslint` 52 problemas (47 errores, 5 avisos).
- `D:\anonimizador` al empezar: ` M src/interfaz/main.test.ts` y `?? …/~$so-con-partes.docx`, HEAD `55838ee`. A las 11:2x otra sesión hizo el commit `50112ea` allí; esta sesión solo leyó y copió `dist/index.html` y `caso-con-partes.docx` al scratchpad.
- Fase 1 (investigar), commit `e27b002`: `docs/plans/2026-09-24-virtual-1-investigacion.md` (casos, alucinaciones, LOPDP, niveles de datos, modelos, dónde configurar) y `2026-09-24-ai-fluency-resumen.md` (4D, CC BY-NC-SA). Cinco corridas `sonnet`; una se quedó sin contexto y se repitió en una tarea corta. Sigue: fase 2, plan de slides.
- Fase 2 (planear): `docs/plans/2026-09-24-virtual-1-plan.md`, 43 slides en el módulo `v1-virtual-1`, selector en `client/src/data/cursos.js`. El `Plan` (`opus`) corrió en solo lectura y devolvió el plan como texto; el controlador lo copió tal cual de su transcripción. Capturas del Anonimizador hechas con Playwright sobre una copia en el scratchpad. Sigue: fase 3, ejecutor `sonnet`.
- Fase 3 (ejecutar), commit `d8c7657`: selector `VITE_COURSE` (`client/src/data/cursos.js`, `App.jsx:3`), `client/src/data/avanzado/` con 43 slides y 5 materiales en `client/public/materiales/`. Ejecutor `sonnet`, sin desvíos del plan. Build pasa con y sin la variable; eslint 52. Sigue: fase 4, recorrido y capturas.
- Fase 4 (verificar): build y eslint (52) sin cambios. Recorrido de los 43 slides con Playwright headless sobre el build `VITE_COURSE=avanzado`, en 1280×800 y 375×812, con Supabase bloqueado para no escribir `session_state` en modo admin: sin errores de página ni desborde horizontal. En móvil las tablas `comparison` se desplazan hacia el lado (la última columna no se ve sin deslizar). Básico sin la variable: Módulo 1, portada y `perfil-joseluis.png` cargan. Capturas en `docs/revision/capturas/`. Sigue: slide nuevo con AA-Omniscience (pedido de José Luis a mitad de sesión) y fase 5, revisión.
- Agregado a pedido de José Luis (archivo `D:\curso-ia-avanzado\alucinaciones_benchmarks_IA.md`): slide `v1-3-5b` con AA-Omniscience y un párrafo en `tutorial-evitar-alucinaciones.md`. Cifras verificadas en el sitio (Parte E de la investigación). Su caso Opus 5.5 (high) no existe en el sitio; se usaron Fable 5.1 (max), Opus 5.5 (max) y GPT-6 Astra (high). Build pasa, eslint 52. Capturas `15b-*`. Sigue: fase 5.
- Fase 5 (revisar): `superpowers:code-reviewer` (`opus`) sobre `8096b25..HEAD`. Sin bugs; 6 datos corregidos para que calcen con la investigación (Colombia ×2, «exabogada», AA-Omniscience, ChatGPT en la app, entrenamiento de ChatGPT). No aplicado: pizarra y modo libre compartidos con el básico (aceptado en el encargo). Capturas de los 5 slides cambiados rehechas. Sigue: entrega.
- Entrega: `docs/revision/2026-09-24-virtual-1-para-revisar.md` y cuatro líneas en `~/.claude/BITACORA.md`. Encargo siguiente: `docs/prompts/2026-09-24-diseno-avanzado.md` (rediseño solo del avanzado, decidido por José Luis).
- Ajustes pedidos por José Luis al revisar: quitadas seis notas editoriales (`704a123`); las 4D pasan a `v1-2-5`, después de tokens y contexto, con filas en el orden de la sesión; `v1-7-4` usa un extracto adaptado del `CLAUDE.md` de la notaría, sin nombres, trámites ni rutas. Capturas rehechas (44 slides), build pasa, eslint 52.
